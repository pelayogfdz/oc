version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: caanma-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
