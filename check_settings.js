const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const branches = await prisma.branch.findMany({ where: { tenantId: '8b52cbcd-c956-4717-a1bd-02e57386aaa2' } }); for(const b of branches) { const settings = await prisma.branchSettings.findUnique({ where: { branchId: b.id } }); console.log(b.name, settings?.configJson); } } main().catch(console.error).finally(() => prisma.$disconnect());
