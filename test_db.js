const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const branch = await prisma.branch.findFirst({where:{name:'SAN JUAN DEL RIO'}}); console.log('Branch ID:', branch.id); const data = await prisma.inventoryMovement.findMany({ where: { product: { branchId: branch.id }, OR: [ { type: 'ADJUSTMENT' }, { reason: { startsWith: 'Ajuste Inventario' } } ] }, include: { product: true } }); console.log('Items found:', data.length); console.log(data); } main().catch(console.error).finally(() => prisma.$disconnect());
